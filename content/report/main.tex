\documentclass[sigplan,10pt,nonacm]{acmart}

\AtBeginDocument{%
  \providecommand\BibTeX{{Bib\TeX}}}

\title{Recovering TASO Substitutions Under Bounded Rewrite Closure}

\author{Pietro Marsella}
\affiliation{%
  \institution{Stanford University}
  \country{USA}}
\email{marsella@stanford.edu}

\author{Bodo Wirth}
\affiliation{%
  \institution{Stanford University}
  \country{USA}}
\email{bodow@stanford.edu}

\renewcommand{\shortauthors}{Marsella and Wirth}

\begin{document}

\begin{abstract}
TODO: Add abstract (about 100--150 words).
\end{abstract}

% \keywords{graph rewriting, deep learning compilers, TASO, substitution systems}

\maketitle

% Target length guidance for a 4-page report:
% Introduction: 0.5 pages
% Motivation/Background/Related Work: 0.5 pages
% Approach/Implementation: 1.5 pages
% Methodology/Results: 1 page
% Discussion/Limitations/Next Steps: 0.5 pages

\section{Introduction}
TODO

\section{Motivation, Background, and Related Work}
TODO (e.g., TASO~\cite{jia2019taso})

\section{Approach and Implementation}
\subsection{IR}
\subsection{Axioms}
The axioms are strucutred this and that way.
EXAMPLE WITH PICTURE
\subsection{Substitution Engine}
The matching is done by initially matching the ouput tensor of the source of the axiom graph with a tensor of the output graph.
We then recursively match accoring to the following rules. For a given pair of nodes:
- If both are input...
- If src input and dst not input...
- If src not input and dst input...
- If neither is input, then they must be the same expression: we then recurively match on their subexpressions.
(We propagate a partial state as we go along).


However, we still have that... To address this, we perform common subexpression elimination after each rewrite.

\subsection{Proof Search}

One of the main motivations for enforcing a bidireactioanl rewrite system, was, in addition to completeness, the follows:
A nice property of a fully bidirectional rewrite system is that if we have an intermediate state s for which we have sound derivations src*->s and dst*->s, then we can invert the dst->s derivation and then chain it to obtain a sound src*->dst derivation (and viceversa).
GIevn that we are doing combinanorial searhc, this makes the search significnatly faster. Suppose that src and dst are a minium of n rewirets apart, and that each state can be rewritten to x new states (and assue, for simplifty, that these strates had not been visited before). Then, to reach dst from src, we would need to visit at least x^n states (assuming BFS). With the bidirectional approach, we can however intitiate the search from both src and dst, and thus visit at most x^(n/2) states. (We explore until we discover a commomn state.)

\section{Methodology and Results}
TODO

\section{Discussion, Limitations, and Next Steps}
TODO



\subssection

Case study: Tranpose(X) -> X

Surprisignly, this test was initially failing. Turns out, that under the current axioms, the derivation was more complicated than intially anticipated:
TO prove, we neede the following axioms:
matmul(x, I) -> X
matmul(x.T, y.T) -> matmul(y,x)
matmul(...)

transpose(I)                                  start
matmul(transpose(I), I)                       [invertSubstitution axiom26]
matmul(transpose(I), transpose(transpose(I))) [invertSubstitution axiom9]
transpose(matmul(transpose(I), I))            [invertSubstitution axiom16]
transpose(transpose(I))                       [axiom26]
I                                             [axiom9]

TODO: add bidirectional BFS search map under these 3 axioms and show the intersection.

Analogously, we have for matmul(I, x) -> x:

matmul(I, x)                                        start
matmul(transpose(transpose(I)), x)                  [invertSubstitution axiom9]
matmul(transpose(transpose(I)), transpose(transpose(x))) [invertSubstitution axiom9]
transpose(matmul(transpose(x), transpose(I)))       [invertSubstitution axiom16]
transpose(matmul(transpose(x), I))                  [transpose(I) -> I]
transpose(transpose(x))                             [axiom26]
x                                                   [axiom9]

(note that it uses transpose(I) -> I as an axiom, making even the shortest proof several rewrites long).

Surprisignly, the TASO team converged on the same problem, and added Tranpose(X) -> X as a lemma. We adopt the same solution.
(To say that this is not a weakness of rewrites vs proof system, like distance in rewrite space and in proof space are comparable).
\bibliographystyle{ACM-Reference-Format}
\bibliography{refs}

\end{document}

--TODO: figure out if approaching from both directions is the right thing to do.
